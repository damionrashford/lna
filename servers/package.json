{
  "name": "@automo/servers",
  "private": true,
  "type": "module",
  "description": "The local bridge daemon reachable over LNA — spawns processes + stdio MCP servers for AUTOMO",
  "scripts": {
    "bridge": "bun bridge.ts"
  }
}
