{
  "name": "automo",
  "private": true,
  "type": "module",
  "description": "AUTOMO — a local-first browser AI agent that reaches your files, shell, local model, and MCP tools over Local Network Access (Bun workspace)",
  "workspaces": ["web", "servers", "agent"],
  "scripts": {
    "serve": "bun x serve web -l 5173",
    "bridge": "bun servers/bridge.ts",
    "test-daemon": "bun servers/test.ts",
    "agent": "OPENAI_AGENTS_DISABLE_TRACING=1 bun agent/agent.ts"
  }
}
